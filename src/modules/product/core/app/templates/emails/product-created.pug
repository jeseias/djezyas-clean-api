doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Product Created - #{product.name}
    style.
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #f8f9fa;
        padding: 20px;
        text-align: center;
        border-radius: 5px;
      }
      .content {
        padding: 20px;
        background-color: #ffffff;
        border: 1px solid #e9ecef;
        border-radius: 5px;
        margin-top: 20px;
      }
      .product-info {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 5px;
        margin: 15px 0;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        color: #6c757d;
        font-size: 14px;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 5px;
        margin: 10px 0;
      }

  body
    .header
      h1 Product Created Successfully
    
    .content
      p Hello #{userName},
      
      p A new product has been created in your organization:
      
      .product-info
        h3 #{product.name}
        if product.description
          p #{product.description}
        p 
          strong Category: 
          | #{product.category}
        p 
          strong SKU: 
          | #{product.sku || 'Not specified'}
        if product.barcode
          p 
            strong Barcode: 
            | #{product.barcode}
        if product.weight
          p 
            strong Weight: 
            | #{product.weight} kg
      
      p The product is currently in 
        strong #{product.status} 
        | status and can be managed through your organization dashboard.
      
      a.button(href="#{dashboardUrl}/products/#{product.id}") View Product
      
      p
        | If you have any questions, please contact your organization administrator.
    
    .footer
      p This is an automated notification from #{organizationName}
      p Sent on #{createdAt} 